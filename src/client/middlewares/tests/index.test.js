import { logger } from '../index'

describe('Logger:', () => {
    describe('index.js:', () => {
        // GIVEN
        it('ShouldFind be defined', () => {
            expect(logger).toBeDefined();
        });
    });
});